# Shared media storage (outside repo — mount this in Docker for workers & API)
MEDIA_ROOT=/Users/YOUR_USERNAME/Developer/netflix-adaptive-stream-media

# API
PORT=3000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/adaptive_streaming
TEMPORAL_ADDRESS=localhost:7233
TEMPORAL_TASK_QUEUE=video-processing
CORS_ORIGIN=http://localhost:5173

# Worker
FFMPEG_PATH=ffmpeg
FFPROBE_PATH=ffprobe

# Web (Vite)
VITE_API_URL=http://localhost:3000
